plug std::console.println;

func main() {
    println("Hello, World!")
}
/*
visible func add(a: decimal, b: decimal) -> decimal {
    a + b
}
visible func sub(a: decimal, b: decimal) -> decimal {
    a - b
}

visible struct Point {
    x: int,
    y: int,
    ah: omni
}
ext func new(x: int, y: int, ah: omni) for Point -> Point {
    Point { x, y, ah }
} */

//commentariy yomayo
